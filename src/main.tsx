import ReactDOM from "react-dom/client";
import List from "./List.tsx";
import { StrictMode } from "react";

ReactDOM.createRoot(document.getElementById("Types")!).render(
  <StrictMode>
    <List />
  </StrictMode>
);
